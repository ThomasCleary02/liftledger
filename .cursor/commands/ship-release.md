# Ship release

Ship a LiftLedger version: bump semver, commit, tag `vX.Y.Z`, push, and create the GitHub release. Follow `.cursor/skills/ship-release`. Prefer running `/pre-release` first if we have not audited this patch yet. Do this only because I asked to release.
